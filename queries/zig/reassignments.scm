;; ═════════════════════════════════════════════════════════════════════════
;; Zig Reassignment Queries
;; ═════════════════════════════════════════════════════════════════════════
;; Note: Zig's assignment expression is not well-defined at top level.
;; In Zig, variables declared with `var` can be reassigned, but the
;; grammar may vary based on context.
