;; ═════════════════════════════════════════════════════════════════════════
;; Ruby Scope Node Queries
;; ═════════════════════════════════════════════════════════════════════════
;;
;; These patterns identify nodes that create new lexical scopes.

;; ───────────────────────────────────────────────────────────────────────────
;; Methods
;; ───────────────────────────────────────────────────────────────────────────
(method) @scope_node
(singleton_method) @scope_node

;; ───────────────────────────────────────────────────────────────────────────
;; Classes and Modules
;; ───────────────────────────────────────────────────────────────────────────
(class) @scope_node
(module) @scope_node

;; ───────────────────────────────────────────────────────────────────────────
;; Blocks
;; ───────────────────────────────────────────────────────────────────────────
(block) @scope_node
(do_block) @scope_node
(lambda) @scope_node

;; ───────────────────────────────────────────────────────────────────────────
;; Loops
;; ───────────────────────────────────────────────────────────────────────────
(for) @scope_node
(while) @scope_node
(until) @scope_node

;; ───────────────────────────────────────────────────────────────────────────
;; Conditionals
;; ───────────────────────────────────────────────────────────────────────────
(if) @scope_node
(unless) @scope_node
(case) @scope_node
(begin) @scope_node
